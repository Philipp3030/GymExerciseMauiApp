<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GymExerciseMauiApp.AddExercisePage"
             Title="AddExercisePage">
    <ScrollView>
        <StackLayout Padding="20" Spacing="10">
            <!-- Id (Read-Only) -->
            <!--<Label Text="Id" />
            <Entry x:Name="IdEntry" IsReadOnly="True" />-->

            <!-- Musclegroup -->
            <Label Text="Musclegroup" />
            <Picker x:Name="MusclegroupOptions"
                    Title="Select an Option"
                    ItemsSource="{Binding Options}"
                    ItemDisplayBinding="{Binding Name}" >
            </Picker>

            <!-- Name -->
            <Label Text="Name" />
            <Entry x:Name="NameEntry" Placeholder="Enter exercise name" />

            <!-- IsActive -->
            <Label Text="Is Active" />
            <Switch x:Name="IsActiveSwitch" IsToggled="True" />

            <!-- MachineName -->
            <Label Text="Machine Name" />
            <Entry x:Name="MachineNameEntry" Placeholder="Enter machine name (optional)" />

            <!-- Description -->
            <Label Text="Description" />
            <Editor x:Name="DescriptionEditor" Placeholder="Enter description (optional)" />

            <!-- Sets -->
            <Label Text="Sets" />
            <Entry x:Name="SetsEntry" Placeholder="Enter sets (optional)" Keyboard="Numeric" TextChanged="OnEntryTextChanged" />

            <!-- RepsPrevious -->
            <Label Text="Reps Previous" />
            <Entry x:Name="RepsPreviousEntry" Placeholder="Enter previous reps (optional)" Keyboard="Numeric" TextChanged="OnEntryTextChanged" />

            <!-- Reps -->
            <Label Text="Reps" />
            <Entry x:Name="RepsEntry" Placeholder="Enter reps (optional)" Keyboard="Numeric" TextChanged="OnEntryTextChanged" />

            <!-- RepsGoal -->
            <Label Text="Reps Goal" />
            <Entry x:Name="RepsGoalEntry" Placeholder="Enter goal reps (optional)" Keyboard="Numeric" TextChanged="OnEntryTextChanged" />

            <!-- Optional: Button to Submit -->
            <Button Text="Submit" Clicked="OnSubmitClicked" />

            <Editor
                x:Name="ResultEditor"
                WidthRequest="200"
                HeightRequest="300"
                HorizontalOptions="Center" 
            />
        </StackLayout>
    </ScrollView>
</ContentPage>