<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="GymExerciseMauiApp.SavedTrainingsPage"
             xmlns:viewmodel="clr-namespace:GymExerciseClassLibrary.ViewModels;assembly=GymExerciseClassLibrary"
             x:DataType="viewmodel:TrainingViewModel"
             Title="SavedTrainings">
    <!--<CollectionView ItemsSource="{Binding AllTrainingVMs}" x:Name="TrainingCollectionView" >
        <CollectionView.ItemTemplate>
            <DataTemplate>

                <Frame BorderColor="LightGray" Margin="5" Padding="10" >
                    <Frame.Behaviors>
                        <toolkit:TouchBehavior DefaultAnimationDuration="250" HoveredBackgroundColor="{StaticResource Gray900}" TouchGestureCompleted="OnLabelTapped" />
                    </Frame.Behaviors>
                    <VerticalStackLayout>
                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" />
                        <Label Text="{Binding Description}" FontSize="14" TextColor="Gray" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>-->
</ContentPage>